const errorMessages = {
  required: "This field is required",
  invalidEmail: "Invalid e-mail",
  matchPasswords: "Passwords should match",
  invalidCredentials: "Invalid credentials!",
};

export default errorMessages;
